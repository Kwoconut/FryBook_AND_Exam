<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frybl">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.Activities.SignUpActivity"></activity>
        <activity android:name=".View.Activities.LoginActivity" />
        <activity android:name=".View.Activities.SavedRecipesActivity" />
        <activity android:name=".View.Activities.SavedRecipeDetailsActivity" />
        <activity android:name=".View.Activities.UploadDetailsActivity" />
        <activity android:name=".View.Activities.AddRecipeActivity" />
        <activity android:name=".View.Activities.UserUploadsActivity" />
        <activity android:name=".View.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>